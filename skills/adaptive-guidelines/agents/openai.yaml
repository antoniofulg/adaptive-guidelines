interface:
  display_name: "Adaptive Guidelines"
  short_description: "Reuse lessons and suggest skills from recurring workflows"
  default_prompt: "Use $adaptive-guidelines to consult relevant saved lessons before starting this work."

policy:
  allow_implicit_invocation: true
